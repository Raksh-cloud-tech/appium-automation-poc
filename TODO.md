# TODO: Fix Scrolling Issues in MyVisitorsPOM.ts

- [x] Update `capturePhotoWithOverride` method to click the "Retake" button directly after scrolling to it
- [x] Update `captureIdWithOverride` method to click the "Over ride" button directly after scrolling to it and fix syntax error in scrollGesture call
- [x] Test the changes by running the VMSMyvisitors.spec.ts test to ensure captures work and verifyDocuments passes
